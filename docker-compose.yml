services:

  backend:
    container_name: backend
    build: .
    volumes:
      - ./config/:/app/
    ports:
      - "8000:8000"
    env_file:
      - ./config/.env
    command: python manage.py runserver 0.0.0.0:8000
